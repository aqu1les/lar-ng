import { SegmentDirective } from './segment.directive';

describe('SegmentDirective', () => {
  it('should create an instance', () => {
    const directive = new SegmentDirective();
    expect(directive).toBeTruthy();
  });
});
